import { useState } from "react";
import { Bell, Edit3, Plus, Trash2 } from "lucide-react";
import { useData } from "../../../context/DataContext";

const blank = { title: "", message: "", type: "INFO" };
const AdminNotificationsPage = () => {
  const { notifications, createNotification, updateNotification, deleteNotification, markNotificationRead, markAllNotificationsRead } = useData();
  const [form, setForm] = useState(blank); const [editingId, setEditingId] = useState(null); const [error, setError] = useState("");
  const submit = async (event) => { event.preventDefault(); try { if (editingId) await updateNotification(editingId, form); else await createNotification(form); setForm(blank); setEditingId(null); } catch (requestError) { setError(requestError.message); } };
  return <section className="space-y-6 pb-12"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-widest text-orange-600">Admin communication</p><h1 className="mt-2 text-3xl font-black text-slate-900">Notification Center</h1><p className="mt-2 text-sm text-slate-500">Create, update, read and delete system notifications.</p></div><button onClick={() => markAllNotificationsRead()} className="rounded-xl border border-orange-200 px-4 py-2 text-sm font-bold text-orange-600">Mark all as read</button></div><form onSubmit={submit} className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm md:grid-cols-[1fr_1fr_160px_auto]"><input required value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} placeholder="Notification title" className="rounded-xl border border-slate-200 px-3 py-2 text-sm" /><input required value={form.message} onChange={(e) => setForm({ ...form, message: e.target.value })} placeholder="Message" className="rounded-xl border border-slate-200 px-3 py-2 text-sm" /><select value={form.type} onChange={(e) => setForm({ ...form, type: e.target.value })} className="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option>INFO</option><option>SUCCESS</option><option>WARNING</option><option>ALERT</option></select><button className="inline-flex items-center justify-center gap-2 rounded-xl bg-orange-500 px-4 py-2 text-sm font-bold text-white"><Plus size={16} />{editingId ? "Update" : "Create"}</button></form>{error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="divide-y divide-slate-100">{notifications.length ? notifications.map((item) => <div key={item.id} className={`flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between ${item.read ? "" : "bg-orange-50/40"}`}><div className="flex gap-3"><span className="mt-1 rounded-xl bg-orange-100 p-2 text-orange-600"><Bell size={17} /></span><div><div className="flex items-center gap-2"><h2 className="font-bold text-slate-900">{item.title}</h2>{!item.read && <span className="rounded-full bg-orange-500 px-2 py-0.5 text-[10px] font-bold text-white">New</span>}</div><p className="mt-1 text-sm text-slate-600">{item.message}</p><p className="mt-1 text-xs text-slate-400">{item.type} · {item.time}</p></div></div><div className="flex gap-2"><button onClick={() => markNotificationRead(item.id)} className="rounded-lg bg-slate-100 px-3 py-2 text-xs font-bold text-slate-700">Read</button><button onClick={() => { setEditingId(item.id); setForm({ title: item.title, message: item.message, type: item.type }); }} title="Edit notification" className="rounded-lg bg-blue-50 p-2 text-blue-600"><Edit3 size={15} /></button><button onClick={() => deleteNotification(item.id)} title="Delete notification" className="rounded-lg bg-red-50 p-2 text-red-600"><Trash2 size={15} /></button></div></div>) : <div className="p-10 text-center text-sm text-slate-500">No notifications found.</div>}</div></div></section>;
};

export default AdminNotificationsPage;