import { useEffect, useState } from "react";
import { apiFetch } from "../../../utils/api";

const TeacherFees = () => { const [fees, setFees] = useState([]); const [error, setError] = useState(""); useEffect(() => { apiFetch("/api/portal/fees").then((response) => setFees(response.data || [])).catch((requestError) => setError(requestError.message)); }, []); return <div className="space-y-6"><div><h1 className="text-3xl font-black text-slate-900">Fees</h1><p className="mt-2 text-sm text-slate-500">Fee status for students in your assigned batches.</p></div>{error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="overflow-hidden rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Student</th><th className="p-4">Total</th><th className="p-4">Paid</th><th className="p-4">Pending</th><th className="p-4">Status</th></tr></thead><tbody className="divide-y">{fees.map((fee) => <tr key={fee._id}><td className="p-4 font-semibold">{fee.studentId?.name || "Student"}</td><td className="p-4">₹{fee.totalAmount || 0}</td><td className="p-4 text-emerald-600">₹{fee.totalPaid || 0}</td><td className="p-4 text-amber-600">₹{fee.totalPending || 0}</td><td className="p-4">{fee.status}</td></tr>)}</tbody></table>{!fees.length && !error && <p className="p-8 text-center text-sm text-slate-500">No fee records found.</p>}</div></div>; };
export default TeacherFees;
