import { createSlice } from "@reduxjs/toolkit"; import * as t from "./dashboardThunks"; import { resetServerState } from "../reset";
const dashboard = createSlice({ name: "dashboard", initialState: { data: null, loading: false, error: null }, reducers: {}, extraReducers: (b) => { Object.values(t).forEach((thunk) => b.addCase(thunk.pending, (s) => { s.loading = true; s.error = null; }).addCase(thunk.fulfilled, (s, a) => { s.loading = false; s.data = a.payload; }).addCase(thunk.rejected, (s, a) => { s.loading = false; s.error = a.payload; })); b.addCase(resetServerState, () => ({ data: null, loading: false, error: null })); } }); export default dashboard.reducer;
