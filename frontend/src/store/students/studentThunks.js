import { createAsyncThunk } from "@reduxjs/toolkit"; import { studentService } from "../../services/student.service";
const wrap = (type, task) => createAsyncThunk(type, async (arg, { rejectWithValue }) => { try { return await task(arg); } catch (e) { return rejectWithValue(e.message || "Student request failed"); } });
export const fetchStudents = wrap("students/fetchStudents", (params) => studentService.getStudents(params)); export const fetchStudentById = wrap("students/fetchStudentById", (id) => studentService.getStudentById(id)); export const createStudent = wrap("students/createStudent", (payload) => studentService.createStudent(payload)); export const updateStudent = wrap("students/updateStudent", ({ id, payload }) => studentService.updateStudent(id, payload)); export const deleteStudent = wrap("students/deleteStudent", (id) => studentService.deleteStudent(id)); export const updateStudentStatus = wrap("students/updateStudentStatus", ({ id, payload }) => studentService.updateStudentStatus(id, payload));
