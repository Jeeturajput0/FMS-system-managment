import { createAsyncThunk } from "@reduxjs/toolkit"; import { courseService } from "../../services/course.service";
const wrap = (type, task) => createAsyncThunk(type, async (arg, { rejectWithValue }) => { try { return await task(arg); } catch (e) { return rejectWithValue(e.message || "Course request failed"); } });
export const fetchCourses = wrap("courses/fetchCourses", (params) => courseService.getCourses(params)); export const fetchCourseById = wrap("courses/fetchCourseById", (id) => courseService.getCourseById(id)); export const createCourse = wrap("courses/createCourse", (payload) => courseService.createCourse(payload)); export const updateCourse = wrap("courses/updateCourse", ({ id, payload }) => courseService.updateCourse(id, payload)); export const deleteCourse = wrap("courses/deleteCourse", (id) => courseService.deleteCourse(id));
