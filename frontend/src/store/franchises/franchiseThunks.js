import { createAsyncThunk } from "@reduxjs/toolkit"; import { franchiseService } from "../../services/franchise.service";
const wrap = (type, task) => createAsyncThunk(type, async (arg, { rejectWithValue }) => { try { return await task(arg); } catch (e) { return rejectWithValue(e.message || "Franchise request failed"); } });
export const fetchFranchises = wrap("franchises/fetchFranchises", () => franchiseService.getFranchises()); export const fetchFranchiseById = wrap("franchises/fetchFranchiseById", (id) => franchiseService.getFranchiseById(id)); export const createFranchise = wrap("franchises/createFranchise", (payload) => franchiseService.createFranchise(payload)); export const updateFranchise = wrap("franchises/updateFranchise", ({ id, payload }) => franchiseService.updateFranchise(id, payload)); export const deleteFranchise = wrap("franchises/deleteFranchise", (id) => franchiseService.deleteFranchise(id));
