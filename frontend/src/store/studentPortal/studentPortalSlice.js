import { createSlice } from "@reduxjs/toolkit"; import { fetchStudentPortal, fetchStudentAttendance, fetchStudentAssignments } from "./studentPortalThunks"; import { fetchCurrentUser } from "../auth/authThunks"; import { resetServerState } from "../reset";
const initialState = { dashboard: null, certificate: null, courses: [], fees: [], attendance: [], assignments: [], loading: false, error: null };
const slice = createSlice({ name: "studentPortal", initialState, reducers: {}, extraReducers: (b) => b.addCase(fetchStudentPortal.pending, (s) => { s.loading = true; s.error = null; }).addCase(fetchStudentPortal.fulfilled, (s, a) => Object.assign(s, a.payload, { loading: false })).addCase(fetchStudentPortal.rejected, (s, a) => { s.loading = false; s.error = a.payload; }).addCase(fetchStudentAttendance.fulfilled, (s, a) => { s.attendance = a.payload; }).addCase(fetchStudentAssignments.fulfilled, (s, a) => { s.assignments = a.payload; }).addCase(resetServerState, () => initialState) }); export default slice.reducer;
